<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Assignment
  body
    p User List
    if users.length < 1
      | "Empty users"
    else
      ul
        each u in users
          li #{u}
